name: Publish Wiki

on:
  push:
    branches: [preview]
    paths:
      - '.github/wiki/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Publish wiki pages
        uses: Andrew-Chen-Wang/github-wiki-action@v4
        with:
          path: .github/wiki/
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: sync wiki from .github/wiki/"
